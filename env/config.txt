# env-management-template config_template.txt
# Copy this file to env/config.txt and customise as needed.
# Only redefine variables you want to change.

# ── System packages ───────────────────────────────────────────────
# direnv, python3, python3-venv and lmod are always required
DNF_PACKAGES=(direnv python3 python3-venv lmod R R-devel)
YUM_PACKAGES=("${DNF_PACKAGES[@]}")                   # RHEL <8 same names
APT_PACKAGES=(direnv python3 python3-venv lmod r-base r-base-dev build-essential gfortran gcc cmake git wget curl python3 ruby-full numdiff libfontconfig1-dev libfreetype6-dev libharfbuzz-dev libfribidi-dev libpng-dev libtiff5-dev libjpeg-dev)
PACMAN_PACKAGES=(untested)                                   # untested means not supported by repository developer - use at your own risk

# ── Python packages (pip) ─────────────────────────────────────────
PYTHON_PACKAGES=(numpy)

# ── Optional: R packages to pre-seed into renv cache ─────────────
R_PACKAGES=(renv data.table fuzzyjoin tidyverse lookup plotly)

# ── Interpreter version pins (optional) ──────────────────────────
# Leave blank to use the system default versions.
# PYTHON_VERSION=3.11
# R_VERSION=4.3.2

# ── Lmod / OpenHPC modules to load automatically ─────────────────
OPENHPC_MODULES=()

# Extensions and folders to add to local .gitignore
# Combined with defaults from setup_env.sh
GITIGNORE_ADD=(atchem-lib .gem src/dataStructures.o tests/model_tests/spec_model_kpp/spec_model_kpp.fac isopleth/output/* model/configuration/* model/output*)
